// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/manhrev/runtracking/backend/event/pkg/ent/groupprogress"
	"github.com/manhrev/runtracking/backend/event/pkg/ent/memberprogress"
	"github.com/manhrev/runtracking/backend/event/pkg/ent/predicate"
)

// MemberProgressQuery is the builder for querying MemberProgress entities.
type MemberProgressQuery struct {
	config
	ctx        *QueryContext
	order      []memberprogress.Order
	inters     []Interceptor
	predicates []predicate.MemberProgress
	withGroup  *GroupProgressQuery
	withFKs    bool
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MemberProgressQuery builder.
func (mpq *MemberProgressQuery) Where(ps ...predicate.MemberProgress) *MemberProgressQuery {
	mpq.predicates = append(mpq.predicates, ps...)
	return mpq
}

// Limit the number of records to be returned by this query.
func (mpq *MemberProgressQuery) Limit(limit int) *MemberProgressQuery {
	mpq.ctx.Limit = &limit
	return mpq
}

// Offset to start from.
func (mpq *MemberProgressQuery) Offset(offset int) *MemberProgressQuery {
	mpq.ctx.Offset = &offset
	return mpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mpq *MemberProgressQuery) Unique(unique bool) *MemberProgressQuery {
	mpq.ctx.Unique = &unique
	return mpq
}

// Order specifies how the records should be ordered.
func (mpq *MemberProgressQuery) Order(o ...memberprogress.Order) *MemberProgressQuery {
	mpq.order = append(mpq.order, o...)
	return mpq
}

// QueryGroup chains the current query on the "group" edge.
func (mpq *MemberProgressQuery) QueryGroup() *GroupProgressQuery {
	query := (&GroupProgressClient{config: mpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(memberprogress.Table, memberprogress.FieldID, selector),
			sqlgraph.To(groupprogress.Table, groupprogress.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, memberprogress.GroupTable, memberprogress.GroupColumn),
		)
		fromU = sqlgraph.SetNeighbors(mpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first MemberProgress entity from the query.
// Returns a *NotFoundError when no MemberProgress was found.
func (mpq *MemberProgressQuery) First(ctx context.Context) (*MemberProgress, error) {
	nodes, err := mpq.Limit(1).All(setContextOp(ctx, mpq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{memberprogress.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mpq *MemberProgressQuery) FirstX(ctx context.Context) *MemberProgress {
	node, err := mpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MemberProgress ID from the query.
// Returns a *NotFoundError when no MemberProgress ID was found.
func (mpq *MemberProgressQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = mpq.Limit(1).IDs(setContextOp(ctx, mpq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{memberprogress.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mpq *MemberProgressQuery) FirstIDX(ctx context.Context) int64 {
	id, err := mpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MemberProgress entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MemberProgress entity is found.
// Returns a *NotFoundError when no MemberProgress entities are found.
func (mpq *MemberProgressQuery) Only(ctx context.Context) (*MemberProgress, error) {
	nodes, err := mpq.Limit(2).All(setContextOp(ctx, mpq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{memberprogress.Label}
	default:
		return nil, &NotSingularError{memberprogress.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mpq *MemberProgressQuery) OnlyX(ctx context.Context) *MemberProgress {
	node, err := mpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MemberProgress ID in the query.
// Returns a *NotSingularError when more than one MemberProgress ID is found.
// Returns a *NotFoundError when no entities are found.
func (mpq *MemberProgressQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = mpq.Limit(2).IDs(setContextOp(ctx, mpq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{memberprogress.Label}
	default:
		err = &NotSingularError{memberprogress.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mpq *MemberProgressQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := mpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MemberProgresses.
func (mpq *MemberProgressQuery) All(ctx context.Context) ([]*MemberProgress, error) {
	ctx = setContextOp(ctx, mpq.ctx, "All")
	if err := mpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MemberProgress, *MemberProgressQuery]()
	return withInterceptors[[]*MemberProgress](ctx, mpq, qr, mpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mpq *MemberProgressQuery) AllX(ctx context.Context) []*MemberProgress {
	nodes, err := mpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MemberProgress IDs.
func (mpq *MemberProgressQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if mpq.ctx.Unique == nil && mpq.path != nil {
		mpq.Unique(true)
	}
	ctx = setContextOp(ctx, mpq.ctx, "IDs")
	if err = mpq.Select(memberprogress.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mpq *MemberProgressQuery) IDsX(ctx context.Context) []int64 {
	ids, err := mpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mpq *MemberProgressQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mpq.ctx, "Count")
	if err := mpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mpq, querierCount[*MemberProgressQuery](), mpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mpq *MemberProgressQuery) CountX(ctx context.Context) int {
	count, err := mpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mpq *MemberProgressQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mpq.ctx, "Exist")
	switch _, err := mpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mpq *MemberProgressQuery) ExistX(ctx context.Context) bool {
	exist, err := mpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MemberProgressQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mpq *MemberProgressQuery) Clone() *MemberProgressQuery {
	if mpq == nil {
		return nil
	}
	return &MemberProgressQuery{
		config:     mpq.config,
		ctx:        mpq.ctx.Clone(),
		order:      append([]memberprogress.Order{}, mpq.order...),
		inters:     append([]Interceptor{}, mpq.inters...),
		predicates: append([]predicate.MemberProgress{}, mpq.predicates...),
		withGroup:  mpq.withGroup.Clone(),
		// clone intermediate query.
		sql:  mpq.sql.Clone(),
		path: mpq.path,
	}
}

// WithGroup tells the query-builder to eager-load the nodes that are connected to
// the "group" edge. The optional arguments are used to configure the query builder of the edge.
func (mpq *MemberProgressQuery) WithGroup(opts ...func(*GroupProgressQuery)) *MemberProgressQuery {
	query := (&GroupProgressClient{config: mpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mpq.withGroup = query
	return mpq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		MemberID int64 `json:"member_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MemberProgress.Query().
//		GroupBy(memberprogress.FieldMemberID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (mpq *MemberProgressQuery) GroupBy(field string, fields ...string) *MemberProgressGroupBy {
	mpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MemberProgressGroupBy{build: mpq}
	grbuild.flds = &mpq.ctx.Fields
	grbuild.label = memberprogress.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		MemberID int64 `json:"member_id,omitempty"`
//	}
//
//	client.MemberProgress.Query().
//		Select(memberprogress.FieldMemberID).
//		Scan(ctx, &v)
//
func (mpq *MemberProgressQuery) Select(fields ...string) *MemberProgressSelect {
	mpq.ctx.Fields = append(mpq.ctx.Fields, fields...)
	sbuild := &MemberProgressSelect{MemberProgressQuery: mpq}
	sbuild.label = memberprogress.Label
	sbuild.flds, sbuild.scan = &mpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MemberProgressSelect configured with the given aggregations.
func (mpq *MemberProgressQuery) Aggregate(fns ...AggregateFunc) *MemberProgressSelect {
	return mpq.Select().Aggregate(fns...)
}

func (mpq *MemberProgressQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mpq); err != nil {
				return err
			}
		}
	}
	for _, f := range mpq.ctx.Fields {
		if !memberprogress.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mpq.path != nil {
		prev, err := mpq.path(ctx)
		if err != nil {
			return err
		}
		mpq.sql = prev
	}
	return nil
}

func (mpq *MemberProgressQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MemberProgress, error) {
	var (
		nodes       = []*MemberProgress{}
		withFKs     = mpq.withFKs
		_spec       = mpq.querySpec()
		loadedTypes = [1]bool{
			mpq.withGroup != nil,
		}
	)
	if mpq.withGroup != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, memberprogress.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MemberProgress).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MemberProgress{config: mpq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(mpq.modifiers) > 0 {
		_spec.Modifiers = mpq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := mpq.withGroup; query != nil {
		if err := mpq.loadGroup(ctx, query, nodes, nil,
			func(n *MemberProgress, e *GroupProgress) { n.Edges.Group = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (mpq *MemberProgressQuery) loadGroup(ctx context.Context, query *GroupProgressQuery, nodes []*MemberProgress, init func(*MemberProgress), assign func(*MemberProgress, *GroupProgress)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*MemberProgress)
	for i := range nodes {
		if nodes[i].group_progress_member == nil {
			continue
		}
		fk := *nodes[i].group_progress_member
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(groupprogress.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "group_progress_member" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (mpq *MemberProgressQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mpq.querySpec()
	if len(mpq.modifiers) > 0 {
		_spec.Modifiers = mpq.modifiers
	}
	_spec.Node.Columns = mpq.ctx.Fields
	if len(mpq.ctx.Fields) > 0 {
		_spec.Unique = mpq.ctx.Unique != nil && *mpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mpq.driver, _spec)
}

func (mpq *MemberProgressQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(memberprogress.Table, memberprogress.Columns, sqlgraph.NewFieldSpec(memberprogress.FieldID, field.TypeInt64))
	_spec.From = mpq.sql
	if unique := mpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mpq.path != nil {
		_spec.Unique = true
	}
	if fields := mpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, memberprogress.FieldID)
		for i := range fields {
			if fields[i] != memberprogress.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mpq *MemberProgressQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mpq.driver.Dialect())
	t1 := builder.Table(memberprogress.Table)
	columns := mpq.ctx.Fields
	if len(columns) == 0 {
		columns = memberprogress.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mpq.sql != nil {
		selector = mpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mpq.ctx.Unique != nil && *mpq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range mpq.modifiers {
		m(selector)
	}
	for _, p := range mpq.predicates {
		p(selector)
	}
	for _, p := range mpq.order {
		p(selector)
	}
	if offset := mpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mpq *MemberProgressQuery) Modify(modifiers ...func(s *sql.Selector)) *MemberProgressSelect {
	mpq.modifiers = append(mpq.modifiers, modifiers...)
	return mpq.Select()
}

// MemberProgressGroupBy is the group-by builder for MemberProgress entities.
type MemberProgressGroupBy struct {
	selector
	build *MemberProgressQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mpgb *MemberProgressGroupBy) Aggregate(fns ...AggregateFunc) *MemberProgressGroupBy {
	mpgb.fns = append(mpgb.fns, fns...)
	return mpgb
}

// Scan applies the selector query and scans the result into the given value.
func (mpgb *MemberProgressGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mpgb.build.ctx, "GroupBy")
	if err := mpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MemberProgressQuery, *MemberProgressGroupBy](ctx, mpgb.build, mpgb, mpgb.build.inters, v)
}

func (mpgb *MemberProgressGroupBy) sqlScan(ctx context.Context, root *MemberProgressQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mpgb.fns))
	for _, fn := range mpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mpgb.flds)+len(mpgb.fns))
		for _, f := range *mpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MemberProgressSelect is the builder for selecting fields of MemberProgress entities.
type MemberProgressSelect struct {
	*MemberProgressQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mps *MemberProgressSelect) Aggregate(fns ...AggregateFunc) *MemberProgressSelect {
	mps.fns = append(mps.fns, fns...)
	return mps
}

// Scan applies the selector query and scans the result into the given value.
func (mps *MemberProgressSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mps.ctx, "Select")
	if err := mps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MemberProgressQuery, *MemberProgressSelect](ctx, mps.MemberProgressQuery, mps, mps.inters, v)
}

func (mps *MemberProgressSelect) sqlScan(ctx context.Context, root *MemberProgressQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mps.fns))
	for _, fn := range mps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mps *MemberProgressSelect) Modify(modifiers ...func(s *sql.Selector)) *MemberProgressSelect {
	mps.modifiers = append(mps.modifiers, modifiers...)
	return mps
}
